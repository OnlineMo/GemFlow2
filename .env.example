# GemFlow2 环境变量示例（复制为 .env 并按需修改）

# 可选：Gemini API（留空则使用启发式）
GEMINI_API_KEY=
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-2.5-pro
GEMINI_TEMPERATURE=0.3

# 速率与并发限制
RATE_LIMIT_RPM=300
RATE_LIMIT_TPM=80000
RATE_LIMIT_CONCURRENT=4

# 超时与重试（毫秒）
TIMEOUT_PER_STEP_MS=30000
TIMEOUT_TOTAL_MS=120000
RETRY_MAX=3
RETRY_INITIAL_MS=500
RETRY_FACTOR=2

# 目标库 B 路径（默认写入仓库内 DeepResearch-Archive/）
REPO_B_DIR=./DeepResearch-Archive

# 时区与运行模式
TZ=Asia/Shanghai
DRY_RUN=false

# 主题数量与 README 当日展示上限
MAX_TOPICS=5
README_TODAY_MAX=20

# 可选：第三方热榜 API Key（如 TianAPI）
TIANAPI_KEY=

# 说明：
# - 若不设置 GEMINI_API_KEY/GOOGLE_API_KEY，将使用启发式进行主题抽取与分类
# - 推荐在本地直接运行：npm run dev（或 node dist/cli.cjs --mode daily）